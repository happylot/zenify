import { AppShell } from "@/components/app-shell";

export default function AppAutomationPage() {
  return (
    <AppShell
      title="Automation"
      description="No-code routing, lifecycle rules, SLA triggers, and AI workflow execution belong here."
    />
  );
}
