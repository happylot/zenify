import { AppShell } from "@/components/app-shell";

export default function AppInboxPage() {
  return (
    <AppShell
      title="Inbox"
      description="Unified conversation handling across chat, email, messaging channels, and voice."
    />
  );
}
