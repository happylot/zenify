import { AppShell } from "@/components/app-shell";

export default function AppBillingPage() {
  return (
    <AppShell
      title="Billing"
      description="Seat counts, invoices, renewals, and upgrades should be managed from this workspace area."
    />
  );
}
