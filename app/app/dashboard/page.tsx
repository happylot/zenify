import { AppShell } from "@/components/app-shell";

export default function AppDashboardPage() {
  return (
    <AppShell
      title="Dashboard"
      description="Activation status, usage KPIs, team progress, and next-best actions should live here."
    />
  );
}
