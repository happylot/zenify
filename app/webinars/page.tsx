import { PageHero } from "@/components/page-hero";

export default function WebinarsPage() {
  return (
    <main>
      <PageHero
        eyebrow="Webinars"
        title="Educational demand capture"
        description="This route is intended for webinars, event archives, and product education content that feeds both acquisition and activation."
      />
    </main>
  );
}
