import { PageHero } from "@/components/page-hero";

export default function EnterprisePage() {
  return (
    <main>
      <PageHero
        eyebrow="Enterprise"
        title="Enterprise sales and deployment"
        description="A route reserved for security reviews, procurement needs, implementation support, and large-scale customer operations."
      />
    </main>
  );
}
