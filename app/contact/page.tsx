import { PageHero } from "@/components/page-hero";

export default function ContactPage() {
  return (
    <main>
      <PageHero
        eyebrow="Contact"
        title="Talk to the Zenify team"
        description="Sales, partnerships, and customer success teams can all be routed from this centralized contact page."
      />
    </main>
  );
}
